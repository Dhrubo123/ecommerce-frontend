import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { Pencil, Plus, Search, Trash2 } from 'lucide-react'
import AdminLayout from '../../components/layout/AdminLayout'
import { deleteSupplier, getSuppliers } from '../../services/supplierService'
import '../brands/brands.css'
export default function SupplierList(){const[items,setItems]=useState([]),[search,setSearch]=useState(''),[error,setError]=useState('');const load=()=>getSuppliers({search}).then(setItems).catch(e=>setError(e.response?.data?.message||'Unable to load suppliers.'));useEffect(()=>{load()},[search]);return <AdminLayout title="Suppliers"><div className="brand-page"><div className="brand-heading"><div><p>INVENTORY & PURCHASE</p><h2>Suppliers</h2><span>Manage supplier contacts for purchasing and inventory.</span></div><Link className="brand-primary" to="/suppliers/create"><Plus size={17}/>Add Supplier</Link></div><section className="brand-card"><div className="brand-toolbar"><label><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search suppliers"/></label></div>{error&&<div className="brand-error">{error}</div>}<div className="brand-table"><table><thead><tr><th>Supplier</th><th>Contact Person</th><th>Phone</th><th>Email</th><th>Address</th><th>Status</th><th>Action</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.name}</strong></td><td>{x.contactPerson}</td><td>{x.phone}</td><td>{x.email}</td><td>{x.address}</td><td><span className={`brand-status ${x.isActive?'active':'inactive'}`}>{x.isActive?'active':'inactive'}</span></td><td><div className="brand-actions"><Link to={`/suppliers/${x.id}/edit`}><Pencil size={16}/></Link><button onClick={async()=>{if(confirm('Delete this supplier?')){await deleteSupplier(x.id);load()}}}><Trash2 size={16}/></button></div></td></tr>)}</tbody></table></div></section></div></AdminLayout>}
