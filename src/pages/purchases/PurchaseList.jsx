import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { Plus, Search } from 'lucide-react'
import AdminLayout from '../../components/layout/AdminLayout'
import { getPurchases } from '../../services/purchaseService'
import '../brands/brands.css'
export default function PurchaseList(){const[items,setItems]=useState([]),[error,setError]=useState(''),[search,setSearch]=useState('');useEffect(()=>{getPurchases({search}).then(setItems).catch(e=>setError(e.response?.data?.message||'Unable to load purchases.'))},[search]);return <AdminLayout title="Purchase List"><div className="brand-page"><div className="brand-heading"><div><p>INVENTORY & PURCHASE</p><h2>Purchase List</h2><span>Review stock purchases and payment amounts.</span></div><Link className="brand-primary" to="/purchases/create"><Plus size={17}/>Add Purchase</Link></div><section className="brand-card"><div className="brand-toolbar"><label><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search invoice number"/></label></div>{error&&<div className="brand-error">{error}</div>}<div className="brand-table"><table><thead><tr><th>Invoice</th><th>Supplier</th><th>Warehouse</th><th>Purchase Date</th><th>Total</th><th>Paid</th><th>Due</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.invoiceNumber}</strong></td><td>{x.supplier?.name??x.supplierName}</td><td>{x.warehouse?.name??x.warehouseName}</td><td>{x.purchaseDate}</td><td>{x.totalAmount??x.total}</td><td>{x.paidAmount}</td><td>{x.dueAmount??((x.totalAmount??0)-(x.paidAmount??0))}</td></tr>)}</tbody></table></div></section></div></AdminLayout>}
