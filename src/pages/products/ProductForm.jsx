import { useEffect, useState } from 'react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import AdminLayout from '../../components/layout/AdminLayout'
import { createProduct, getProduct, updateProduct } from '../../services/productService'
import { getBrands } from '../../services/brandService'
import { getCategories } from '../../services/categoryService'
import { getColors } from '../../services/colorService'
import { getSizes } from '../../services/sizeService'
import { getUnits } from '../../services/unitService'
import './products.css'
import './product-select.css'

const blank = { name:'', slug:'', shortDescription:'', description:'', categoryId:'', subcategoryId:'', brandId:'', colorIds:[], unitId:'', sizeIds:[], sku:'', weightKg:'', buyingPrice:'', sellingPrice:'', discountType:'percent', discount:'', stockQuantity:'', metaTitle:'', metaDescription:'', metaKeywords:'', isActive:true, thumbnailImages:[], additionalImages:[] }
const slugify = (value) => value.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')
export default function ProductForm(){ const { id }=useParams(); const nav=useNavigate(); const edit=Boolean(id); const [form,setForm]=useState(blank); const [options,setOptions]=useState({categories:[],brands:[],colors:[],sizes:[],units:[]}); const [error,setError]=useState(''); const [slugEdited,setSlugEdited]=useState(false); useEffect(()=>{ Promise.all([getCategories(),getBrands(),getColors(),getSizes(),getUnits()]).then(([categories,brands,colors,sizes,units])=>setOptions({categories,brands,colors,sizes,units})).catch(()=>setError('Unable to load product attributes.')); if(edit)getProduct(id).then((p)=>setForm({...blank,...p,isActive:p.status==='active'})).catch(()=>setError('Unable to load product.')) },[id,edit]); const change=(e)=>{const{name,value,type,checked,files}=e.target;if(name==='slug')setSlugEdited(true);setForm(x=>({...x,[name]:files?Array.from(files):type==='checkbox'?checked:value,...(name==='name'&&!slugEdited?{slug:slugify(value)}:{})}));setError('')};const toggle=(name,value)=>setForm(x=>({...x,[name]:x[name].includes(value)?x[name].filter(v=>v!==value):[...x[name],value]}));const save=async(e)=>{e.preventDefault();if(!form.name||!form.slug||!form.categoryId||!form.sku)return setError('Name, slug, category, and SKU are required.');try{await(edit?updateProduct(id,form):createProduct(form));nav('/products')}catch(err){setError(err.response?.data?.message||'Unable to save product.')}};return <AdminLayout title={edit?'Edit Product':'Add Product'}><div className="product-page"><div className="product-crumb"><Link to="/dashboard">Dashboard</Link> / <Link to="/products">Products</Link> / {edit?'Edit Product':'Add Product'}</div><div className="product-heading"><div><h2>{edit?'Edit Product':'Add Product'}</h2><span>Create a complete physical product and its catalog metadata.</span></div></div><form className="product-form" onSubmit={save}><main>{error&&<div className="brand-error">{error}</div>}<section><h3>Basic information</h3><Field label="Product Name"><input name="name" value={form.name} onChange={change}/></Field><div className="two"><Field label="Slug"><input name="slug" value={form.slug} onChange={change}/></Field><Field label="SKU"><input name="sku" value={form.sku} onChange={change}/></Field></div><Field label="Short Description"><input name="shortDescription" value={form.shortDescription} onChange={change}/></Field><Field label="Description"><textarea name="description" value={form.description} onChange={change} rows="4"/></Field><div className="three"><Select label="Category" name="categoryId" value={form.categoryId} onChange={change} items={options.categories}/><Select label="Brand" name="brandId" value={form.brandId} onChange={change} items={options.brands}/><Select label="Unit" name="unitId" value={form.unitId} onChange={change} items={options.units}/></div></section><section><h3>Pricing & inventory</h3><div className="three"><Field label="Buying Price"><input type="number" name="buyingPrice" value={form.buyingPrice} onChange={change}/></Field><Field label="Selling Price"><input type="number" name="sellingPrice" value={form.sellingPrice} onChange={change}/></Field><Field label="Stock Quantity"><input type="number" name="stockQuantity" value={form.stockQuantity} onChange={change}/></Field></div><div className="three"><Field label="Weight (KG)"><input type="number" step="0.001" name="weightKg" value={form.weightKg} onChange={change}/></Field><Field label="Discount Type"><select name="discountType" value={form.discountType} onChange={change}><option value="percent">Percent</option><option value="flat">Flat</option></select></Field><Field label="Discount"><input type="number" name="discount" value={form.discount} onChange={change}/></Field></div></section><section><h3>Variants & images</h3><Multi label="Colors" items={options.colors} values={form.colorIds} toggle={(value)=>toggle('colorIds',value)}/><Multi label="Sizes" items={options.sizes} values={form.sizeIds} toggle={(value)=>toggle('sizeIds',value)}/><div className="two"><Field label="Thumbnail Images"><input type="file" name="thumbnailImages" accept="image/*" multiple onChange={change}/></Field><Field label="Additional Images"><input type="file" name="additionalImages" accept="image/*" multiple onChange={change}/></Field></div></section><section><h3>SEO & publishing</h3><Field label="Meta Title"><input name="metaTitle" value={form.metaTitle} onChange={change}/></Field><Field label="Meta Description"><textarea name="metaDescription" value={form.metaDescription} onChange={change} rows="3"/></Field><Field label="Meta Keywords"><input name="metaKeywords" value={form.metaKeywords} onChange={change} placeholder="iphone, apple, smartphone"/></Field><label className="modal-status"><input type="checkbox" name="isActive" checked={form.isActive} onChange={change}/>Active product</label></section><div className="product-form-actions"><button type="button" onClick={()=>nav('/products')}>Cancel</button><button className="product-primary">{edit?'Update Product':'Save Product'}</button></div></main><aside><span>PRODUCT PREVIEW</span><h3>{form.name||'Product name'}</h3><p>{form.shortDescription||'Product description'}</p><b>${form.sellingPrice||'0.00'}</b><small>{form.stockQuantity||0} in stock</small></aside></form></div></AdminLayout>}
function Field({label,children}){return <label className="p-field"><span>{label}</span>{children}</label>}function Select({label,name,value,onChange,items}){return <Field label={label}><select name={name} value={value} onChange={onChange}><option value="">Select {label}</option>{items.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></Field>}function Multi({label,items,values,toggle}){if(label==='Colors')return <Field label="Select Color"><details className="color-choice"><summary>{values.length?`${values.length} color${values.length>1?'s':''} selected`:'Select Color'}</summary><div>{items.map(x=><label key={x.id}><input type="checkbox" checked={values.includes(x.id)} onChange={()=>toggle(x.id)}/><i style={{background:x.hex??x.code}}/>{x.name}</label>)}</div></details></Field>;const update=(event)=>{const selected=Array.from(event.target.selectedOptions,(option)=>Number(option.value));items.forEach((item)=>{const active=values.includes(item.id);const next=selected.includes(item.id);if(active!==next)toggle(item.id)})};return <Field label="Select Size"><select className="size-choice" multiple value={values.map(String)} onChange={update} size={Math.min(Math.max(items.length,2),5)}>{items.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></Field>}
