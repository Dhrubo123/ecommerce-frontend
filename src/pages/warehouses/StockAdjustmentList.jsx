import{useEffect,useState}from'react';import{Link}from'react-router-dom';import{Plus,Search}from'lucide-react';import AdminLayout from'../../components/layout/AdminLayout';import{getStockAdjustments}from'../../services/stockAdjustmentService';import'../brands/brands.css';export default function StockAdjustmentList(){const[x,setX]=useState([]),[e,setE]=useState(''),[s,setS]=useState('');useEffect(()=>{getStockAdjustments({search:s}).then(setX).catch(a=>setE(a.response?.data?.message||'Unable to load stock adjustments.'))},[s]);return <AdminLayout title="Stock Adjustments"><div className="brand-page"><div className="brand-heading"><div><p>INVENTORY & PURCHASE</p><h2>Stock Adjustments</h2><span>Review inventory corrections and physical-count changes.</span></div><Link className="brand-primary" to="/stock-adjustments/create"><Plus size={17}/>Add Adjustment</Link></div><section className="brand-card"><div className="brand-toolbar"><label><Search size={17}/><input value={s} onChange={a=>setS(a.target.value)} placeholder="Search adjustments"/></label></div>{e&&<div className="brand-error">{e}</div>}<div className="brand-table"><table><thead><tr><th>ID</th><th>Warehouse</th><th>Adjustment Date</th><th>Reason</th><th>Created</th></tr></thead><tbody>{x.map(a=><tr key={a.id}><td>#{a.id}</td><td>{a.warehouse?.name??a.warehouseName}</td><td>{a.adjustmentDate}</td><td>{a.reason}</td><td>{a.createdAt}</td></tr>)}</tbody></table></div></section></div></AdminLayout>}
